# Fetching config via fastly compute service

### Fastly compute service to fetch config and return it from behind .edgecmopute.app domain

## Deployment
`fastly user create --token={FASTLY_API_TOKEN}`

`fastly compute publish -y`

